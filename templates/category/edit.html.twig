{% extends 'base.html.twig' %}

{% block title %}Modifier une catégorie
{% endblock %}

{% block body %}
<div class="col-4">
    <h3>
        <strong>Modifier une catégorie</strong>
    </h3>

    {{ include('category/_form.html.twig', {'button_label': 'Modifier'}) }}

    {{ include('category/_delete_form.html.twig') }}</br>

<a href="{{ path('category_index') }}">
    <strong>Revenir à la liste</strong>
</a>
</div>
{% include 'footer.html.twig' %}{% endblock %}
