{{ form_start(form) }}
{{ form_row(form.libelle, { 'label': 'Libellé' }) }}
{{ form_row(form.category, { 'label': 'Catégorie'}) }}
<button type="submit" class="btn btn-success">{{ button_label|default('Confirmer') }}</button>
{{ form_end(form) }}
